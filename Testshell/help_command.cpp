#include "command.h"

bool HelpCommand::execute(SSD_INTERFACE &ssd, const CommandLine &cli) {
  //shell->printHeader();
  //shell->printTeamInfo();
  //shell->printCommands();
  //shell->printTestScripts();
  return true;
}